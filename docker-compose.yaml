services:
  web:
    image: axiosuk/dts-frontend:0.0.0
    labels:
      - "org.opencontainers.image.version=0.0.0"
    container_name: dts-frontend
    environment:
      - API_BASE_URL=https://api.example.com
    restart: unless-stopped
    ports:
      - "5173:80"
