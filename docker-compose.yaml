services:
  web:
    image: ghcr.io/axiosuk/dts-frontend:1.0.0
    labels:
      - "org.opencontainers.image.version=1.1.0
    container_name: dts-frontend
    environment:
      - API_BASE_URL=https://api.example.com
    restart: unless-stopped
    ports:
      - "5173:80"
